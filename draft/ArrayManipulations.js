   /*
    let ideas = data.ideas;

    console.log(ideas);
    for (var i = 0; i < ideas.length; i++) {
      if (ideas[i].id === id) {
        ideas.splice(i, 1);
      }
    }

    setData({
      ...data,
      ideas: ideas
    });
    */
    